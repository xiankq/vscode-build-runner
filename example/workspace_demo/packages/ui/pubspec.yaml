name: ui
description: UI package with build_runner dependency
publish_to: none

environment:
  sdk: ^3.8.0

resolution: workspace

dependencies:
  core:
    path: ../core
  json_annotation: ^4.11.0

dev_dependencies:
  build_runner: ^2.15.0
  json_serializable: ^6.13.1
